pragma solidity ^0.4.20;
pragma experimental ABIEncoderV2;

contract Main{
    
    function encrypt(address userAddress, string role) internal returns (string){
    }

    function decrypt(string token) internal returns (address){
    }

    // comman man => extra fields to fetch
    function getAllTenders() public returns (address[]){
    }

    function getAllOngoingContracts() public returns (address[]){
    }
}